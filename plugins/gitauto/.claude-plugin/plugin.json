{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "gitauto",
  "description": "Guarded Git delivery commands. Each step (branch, verify, commit, push, open-pr, wait-ci, merge, sync-main, cleanup, tag) is a slash command backed by a deterministic bash helper that enforces branch safety, and `/gitauto:ship` runs the whole flow in order.",
  "version": "0.1.0",
  "author": {
    "name": "Andres Bott",
    "email": "contact@andresbott.com"
  }
}
