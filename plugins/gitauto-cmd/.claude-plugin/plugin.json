{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "gitauto-cmd",
  "description": "Token-cheap Git delivery commands on haiku, backed by bundled scripts. `/gitauto-cmd:branch-out` leaves main; `/gitauto-cmd:ship` runs the full flow through merge, sync, and cleanup, and uses an Opus PR writer only for complex changes.",
  "version": "0.2.0",
  "author": {
    "name": "Andres Bott",
    "email": "contact@andresbott.com"
  }
}
